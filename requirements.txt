# PromptShield — Streamlit Community Cloud 배포용 (Python 3.13 기준)
# 앱 실행에 실제로 필요한 것만 담았습니다.
# 데이터 준비(prepare_data.py)용 패키지는 requirements-dev.txt 를 보세요.

streamlit==1.56.0

# LangChain / LangGraph
langgraph==1.2.11
langchain-core==1.5.6
langchain-openai==1.1.12
langchain-community==0.4.1
langchain-classic==1.0.8
langchain-text-splitters==1.1.2

# 벡터 검색 (사전 생성된 인덱스를 로드만 합니다)
faiss-cpu==1.13.2

# 데이터 처리 / 시각화
pandas==3.0.5
numpy==2.5.2
matplotlib==3.11.1

# 웹 API (Tavily 검색, NVD CVE 조회)
requests==2.34.2

# 환경 변수
python-dotenv==1.2.2
